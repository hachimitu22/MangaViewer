# メディア更新画面

## 画面説明
- メディアの更新を行う。
- 管理者のみ利用可能。

## URL
/edit/メディアID

## クエリパラメータ
なし

## APIエラー時の表示方針
- `PATCH /api/media/{mediaId}`（更新）、`DELETE /api/media/{mediaId}`（削除）、または `POST /api/logout`（ナビゲーターのログアウト）がエラー系レスポンスを返した場合、画面上にエラーメッセージを表示し、ユーザーに再度実行を促す。
