# メディアID（Value Object）

## 同一値のメディアIDは同一と判定される
- 前提
  - 同じ値を持つ2つのメディアIDが存在する
- 操作
  - メディアID同士を比較する
- 結果
  - 2つのメディアIDは同一と判定される

## 異なる値のメディアIDは同一と判定されない
- 前提
  - 異なる値を持つ2つのメディアIDが存在する
- 操作
  - メディアID同士を比較する
- 結果
  - 同一とは判定されない
