# /
get:
  summary: ルート画面遷移
  responses:
    "302":
      description: メディア一覧画面へのリダイレクト
      headers:
        Location:
          description: リダイレクト先URL（`/screen/summary`）
          schema:
            type: string
            enum:
              - /screen/summary
