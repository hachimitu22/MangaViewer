# /
get:
  summary: ルート画面遷移
  responses:
    "302":
      description: 公開導線としてメディア一覧画面へリダイレクト
      headers:
        Location:
          description: リダイレクト先URL（`/screen/summary`）
          schema:
            type: string
            enum:
              - /screen/summary
