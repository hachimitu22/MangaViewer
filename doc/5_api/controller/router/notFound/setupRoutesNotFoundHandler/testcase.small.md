# 未定義ルート共通 404 ハンドラー テスト観点（small）

## small テスト観点
- [画面ルートで未定義パスにアクセスした場合に 404 JSON を返す](#画面ルートで未定義パスにアクセスした場合に-404-json-を返す)
- [API ルートで未定義パスにアクセスした場合に 404 JSON を返す](#api-ルートで未定義パスにアクセスした場合に-404-json-を返す)

---

### 画面ルートで未定義パスにアクセスした場合に 404 JSON を返す
- **前提**
  - `createApp` でアプリケーションを構築する。
- **操作**
  - 既存の screen ルートに一致しないパスへ `GET` する。
- **結果**
  - ステータスコードが `404` になる。
  - レスポンスボディが `{ message: 'Not Found' }` になる。

---

### API ルートで未定義パスにアクセスした場合に 404 JSON を返す
- **前提**
  - `createApp` でアプリケーションを構築する。
- **操作**
  - 既存の api ルートに一致しないパスへ `GET` または対象メソッドでリクエストする。
- **結果**
  - ステータスコードが `404` になる。
  - レスポンスボディが `{ message: 'Not Found' }` になる。
