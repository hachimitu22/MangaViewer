# SessionAuthMiddleware テストケース

## 状態
- セッション認証ミドルウェアは API ドキュメント対象から除外したため、本テストケースは廃止した。
- 既存のセッション認証シナリオ（`session_token` 検証、`401` 応答など）はこの文書では管理しない。

## 今後の扱い
- 認証方式を再導入する場合は、採用方式に合わせてテスト観点を新規作成する。
- 廃止済み観点の流用は行わず、要件・実装・OpenAPI の整合を確認したうえで定義する。
