paths:
  /queue:
    get:
      summary: あとで見る一覧ページ
      security:
        - cookieAuth: []
      responses:
        "200":
          description: あとで見る一覧HTML
          content:
            text/html:
              schema:
                type: string
