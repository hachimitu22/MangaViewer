# /queue
get:
  summary: あとで見る一覧画面
  security:
    - cookieAuth: []
  responses:
    "200":
      description: あとで見る一覧HTML
      content:
        text/html:
          schema:
            type: string
    "301":
      $ref: '../../openapi.yaml#/components/responses/RedirectError'
