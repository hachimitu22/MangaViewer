paths:
  /search:
    get:
      summary: 検索条件入力ページ
      security:
        - cookieAuth: []
      responses:
        "200":
          description: 検索条件入力HTML
          content:
            text/html:
              schema:
                type: string
